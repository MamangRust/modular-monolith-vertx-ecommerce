services:
  postgres:
    image: postgres:17-alpine
    container_name: postgres
    environment:
      POSTGRES_USER: DRAGON
      POSTGRES_PASSWORD: DRAGON
      POSTGRES_DB: ECOMMERCE
      POSTGRES_INITDB_ARGS: "-E UTF8 --locale=C"
      PGDATA: /var/lib/postgresql/data/pgdata
    command:
      - "postgres"
      - "-c"
      - "max_connections=1000"
      - "-c"
      - "shared_buffers=4GB"
      - "-c"
      - "effective_cache_size=12GB"
      - "-c"
      - "maintenance_work_mem=512MB"
      - "-c"
      - "checkpoint_completion_target=0.9"
      - "-c"
      - "wal_buffers=16MB"
      - "-c"
      - "default_statistics_target=100"
      - "-c"
      - "random_page_cost=1.1"
      - "-c"
      - "effective_io_concurrency=200"
      - "-c"
      - "work_mem=32MB"
      - "-c"
      - "min_wal_size=1GB"
      - "-c"
      - "max_wal_size=4GB"
      - "-c"
      - "max_worker_processes=8"
      - "-c"
      - "max_parallel_workers_per_gather=4"
      - "-c"
      - "max_parallel_workers=8"
      - "-c"
      - "max_parallel_maintenance_workers=4"
      - "-c"
      - "log_min_duration_statement=1000"
      - "-c"
      - "log_line_prefix=%t [%p]: [%l-1] user=%u,db=%d,app=%a,client=%h "
      - "-c"
      - "superuser_reserved_connections=10"
      - "-c"
      - "idle_in_transaction_session_timeout=60000"
      - "-c"
      - "statement_timeout=30000"
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U DRAGON -d ECOMMERCE"]
      interval: 5s
      timeout: 5s
      retries: 5

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.15.0
    container_name: postgres_exporter
    restart: unless-stopped

    environment:
      DATA_SOURCE_NAME: "postgresql://DRAGON:DRAGON@postgres:5432/ECOMMERCE?sslmode=disable&connect_timeout=10"
      PG_EXPORTER_AUTO_DISCOVER_DATABASES: "true"
      PG_EXPORTER_DISABLE_DEFAULT_METRICS: "false"
      PG_EXPORTER_DISABLE_SETTINGS_METRICS: "false"

    ports:
      - "9187:9187"

    depends_on:
      - postgres

    networks:
      - app_network

    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 256M
  pgbouncer:
    image: edoburu/pgbouncer:latest
    container_name: pgbouncer_debug
    restart: unless-stopped

    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: DRAGON
      DB_PASSWORD: DRAGON
      DB_NAME: ECOMMERCE

      AUTH_TYPE: scram-sha-256

      POOL_MODE: transaction

      DEFAULT_POOL_SIZE: 200
      MIN_POOL_SIZE: 50
      MAX_DB_CONNECTIONS: 800
      MAX_CLIENT_CONN: 10000
      RESERVE_POOL_SIZE: 100
      RESERVE_POOL_TIMEOUT: 5

      SERVER_IDLE_TIMEOUT: 600
      QUERY_TIMEOUT: 30
      SERVER_CONNECT_TIMEOUT: 10
      SERVER_LOGIN_RETRY: 5

      SERVER_RESET_QUERY: DISCARD ALL
      SERVER_RESET_QUERY_ALWAYS: 0

      TCP_KEEPALIVE: 1
      TCP_KEEPCNT: 5
      TCP_KEEPIDLE: 30
      TCP_KEEPINTVL: 10

      ADMIN_USERS: DRAGON
      STATS_USERS: DRAGON

      # Vert.x clients use unnamed prepared queries with transaction pooling.
      # Disable PgBouncer prepared-statement tracking to avoid cross-query reuse.
      MAX_PREPARED_STATEMENTS: 0

      LOG_CONNECTIONS: 0
      LOG_DISCONNECTIONS: 0
      LOG_POOLER_ERRORS: 1

    ports:
      - "6432:5432"

    depends_on:
      postgres:
        condition: service_healthy

    networks:
      - app_network

    healthcheck:
      test:
        [
          "CMD-SHELL",
          "PGPASSWORD=DRAGON psql -h localhost -p 5432 -U DRAGON -d pgbouncer -c 'SHOW POOLS;' || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  pyroscope:
    image: grafana/pyroscope:latest
    ports:
      - 4040:4040
    networks:
      - app_network

  redis-node-1:
    image: redis:7.4
    container_name: redis-node-1
    command: >
      redis-server
      --port 6379
      --cluster-enabled yes
      --cluster-config-file nodes.conf
      --cluster-node-timeout 5000
      --appendonly yes
      --requirepass dragon_knight
      --masterauth dragon_knight
    networks:
      - app_network
    volumes:
      - redis_node_1_data:/data

  redis-node-2:
    image: redis:7.4
    container_name: redis-node-2
    command: >
      redis-server
      --port 6379
      --cluster-enabled yes
      --cluster-config-file nodes.conf
      --cluster-node-timeout 5000
      --appendonly yes
      --requirepass dragon_knight
      --masterauth dragon_knight
    networks:
      - app_network
    volumes:
      - redis_node_2_data:/data

  redis-node-3:
    image: redis:7.4
    container_name: redis-node-3
    command: >
      redis-server
      --port 6379
      --cluster-enabled yes
      --cluster-config-file nodes.conf
      --cluster-node-timeout 5000
      --appendonly yes
      --requirepass dragon_knight
      --masterauth dragon_knight
    networks:
      - app_network
    volumes:
      - redis_node_3_data:/data

  redis-node-4:
    image: redis:7.4
    container_name: redis-node-4
    command: >
      redis-server
      --port 6379
      --cluster-enabled yes
      --cluster-config-file nodes.conf
      --cluster-node-timeout 5000
      --appendonly yes
      --requirepass dragon_knight
      --masterauth dragon_knight
    networks:
      - app_network
    volumes:
      - redis_node_4_data:/data

  redis-node-5:
    image: redis:7.4
    container_name: redis-node-5
    command: >
      redis-server
      --port 6379
      --cluster-enabled yes
      --cluster-config-file nodes.conf
      --cluster-node-timeout 5000
      --appendonly yes
      --requirepass dragon_knight
      --masterauth dragon_knight
    networks:
      - app_network
    volumes:
      - redis_node_5_data:/data

  redis-node-6:
    image: redis:7.4
    container_name: redis-node-6
    command: >
      redis-server
      --port 6379
      --cluster-enabled yes
      --cluster-config-file nodes.conf
      --cluster-node-timeout 5000
      --appendonly yes
      --requirepass dragon_knight
      --masterauth dragon_knight
    networks:
      - app_network
    volumes:
      - redis_node_6_data:/data

  redis-cluster-creator:
    image: redis:7.4
    container_name: redis-cluster-creator
    networks:
      - app_network
    depends_on:
      - redis-node-1
      - redis-node-2
      - redis-node-3
      - redis-node-4
      - redis-node-5
      - redis-node-6
    entrypoint: >
      sh -c "
      sleep 5 &&
      if redis-cli -a dragon_knight --no-auth-warning -h redis-node-1 -p 6379 cluster info | grep -q 'cluster_state:ok'; then
        echo 'Redis cluster already exists, skipping creation' &&
        exit 0
      fi &&
      echo yes | redis-cli --cluster create
      redis-node-1:6379
      redis-node-2:6379
      redis-node-3:6379
      redis-node-4:6379
      redis-node-5:6379
      redis-node-6:6379
      --cluster-replicas 1
      -a dragon_knight
      "

  node-exporter:
    image: quay.io/prometheus/node-exporter
    container_name: node-exporter
    pid: "host"
    networks:
      - app_network
    volumes:
      - "/:/host:ro,rslave"
    command:
      - "--path.rootfs=/host"

  kafka:
    image: apache/kafka:latest
    container_name: my-kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT
      KAFKA_LOG_DIRS: /tmp/kraft-combined-logs
      KAFKA_CLUSTER_ID: kraft-cluster-01
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
    networks:
      - app_network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server localhost:9092 || exit 1",
        ]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 30s

  prometheus:
    image: prom/prometheus:v2.53.0
    container_name: prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
    volumes:
      - ../../observability/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ../../observability/rules:/etc/prometheus/rules:ro
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"
    depends_on:
      alertmanager:
        condition: service_started
      otel-collector:
        condition: service_started
    networks:
      - app_network

  grafana:
    image: grafana/grafana:11.1.0
    container_name: grafana
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GF_SECURITY_ADMIN_PASSWORD:-admin}
    volumes:
      - grafana-storage:/var/lib/grafana
      - ../../observability/grafana/provisioning:/etc/grafana/provisioning:ro
    ports:
      - "3000:3000"
    depends_on:
      prometheus:
        condition: service_started
    networks:
      - app_network

  kafka-exporter:
    image: danielqsj/kafka-exporter:v1.9.0
    container_name: kafka-exporter
    command:
      - "--kafka.server=kafka:9092"
    ports:
      - "9308:9308"
    depends_on:
      kafka:
        condition: service_healthy
    networks:
      - app_network

  alertmanager:
    image: prom/alertmanager:v0.27.0
    container_name: alertmanager
    env_file: ./docker.env
    volumes:
      - ../../observability/alertmanager.yml:/etc/alertmanager/alertmanager.yml.template:ro
      - ../../observability/alertmanager-entrypoint.sh:/entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/entrypoint.sh"]
    command: []
    ports:
      - "9093:9093"
    networks:
      - app_network

  loki:
    image: grafana/loki:2.9.0
    container_name: loki
    ports:
      - "3100:3100"
    volumes:
      - ../../observability/loki-config.yaml:/etc/loki/local-config.yaml
      - loki-data:/loki
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - app_network

  jaeger:
    image: jaegertracing/all-in-one:1.62.0
    container_name: jaeger
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    ports:
      - "16686:16686"
    networks:
      - app_network

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.104.0
    container_name: otel-collector
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
    volumes:
      - ../../observability/otel-collector.yaml:/etc/otelcol-contrib/config.yaml:ro
    ports:
      - "4317:4317"
      - "4318:4318"
      - "13133:13133"
      - "8889:8889"
    depends_on:
      jaeger:
        condition: service_started
      loki:
        condition: service_started
    networks:
      - app_network

  db-migration:
    build:
      context: ../..
      dockerfile: db-migration/Dockerfile
    image: monolith-ecommerce-grpc-vertx/db-migration:latest
    container_name: db-migration
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USERNAME=DRAGON
      - DB_PASSWORD=DRAGON
      - DB_NAME=ECOMMERCE

  apigateway:
    build:
      context: ../..
      dockerfile: apigateway/Dockerfile
    image: monolith-ecommerce-grpc-vertx/apigateway:latest
    container_name: apigateway
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    ports:
      - "5000:5000"
      - "8091:8091"
    networks:
      - app_network
    depends_on:
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      redis-cluster-creator:
        condition: service_started
      auth:
        condition: service_started
      role:
        condition: service_started
      user:
        condition: service_started
      email:
        condition: service_started
      banner:
        condition: service_started
      cart:
        condition: service_started
      category:
        condition: service_started
      merchant:
        condition: service_started
      merchant_award:
        condition: service_started
      merchant_business:
        condition: service_started
      merchant_detail:
        condition: service_started
      merchant_policy:
        condition: service_started
      order:
        condition: service_started
      order_item:
        condition: service_started
      product:
        condition: service_started
      review:
        condition: service_started
      review_detail:
        condition: service_started
      shipping_address:
        condition: service_started
      slider:
        condition: service_started
      transaction:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - HTTP_PORT=5000
      # Keep production-safe defaults; override explicitly for local E2E if needed.
      - RATE_LIMIT_REQUESTS=${RATE_LIMIT_REQUESTS:-100}
      - RATE_LIMIT_WINDOW_MS=${RATE_LIMIT_WINDOW_MS:-60000}

  auth:
    build:
      context: ../..
      dockerfile: auth/Dockerfile
    image: monolith-ecommerce-grpc-vertx/auth:latest
    container_name: auth
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
      redis-cluster-creator:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_AUTH_PORT:-50051}

  role:
    build:
      context: ../..
      dockerfile: role/Dockerfile
    image: monolith-ecommerce-grpc-vertx/role:latest
    container_name: role
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
      redis-cluster-creator:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_ROLE_PORT:-50052}

  user:
    build:
      context: ../..
      dockerfile: user/Dockerfile
    image: monolith-ecommerce-grpc-vertx/user:latest
    container_name: user
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
      redis-cluster-creator:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=50053

  email:
    build:
      context: ../..
      dockerfile: email/Dockerfile
    image: monolith-ecommerce-grpc-vertx/email:latest
    container_name: email
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - SMTP_SERVER=${SMTP_SERVER}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_FROM=${SMTP_FROM:-no-reply@vertx-ecommerce.com}

  banner:
    build:
      context: ../..
      dockerfile: banner/Dockerfile
    image: monolith-ecommerce-grpc-vertx/banner:latest
    container_name: banner
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_BANNER_PORT:-50064}

  cart:
    build:
      context: ../..
      dockerfile: cart/Dockerfile
    image: monolith-ecommerce-grpc-vertx/cart:latest
    container_name: cart
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_CART_PORT:-50060}

  category:
    build:
      context: ../..
      dockerfile: category/Dockerfile
    image: monolith-ecommerce-grpc-vertx/category:latest
    container_name: category
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_CATEGORY_PORT:-50054}

  merchant:
    build:
      context: ../..
      dockerfile: merchant/Dockerfile
    image: monolith-ecommerce-grpc-vertx/merchant:latest
    container_name: merchant
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
      redis-cluster-creator:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      # Keep merchant isolated from category/user during local deployment.
      - GRPC_PORT=50055

  merchant_award:
    build:
      context: ../..
      dockerfile: merchant_award/Dockerfile
    image: monolith-ecommerce-grpc-vertx/merchant_award:latest
    container_name: merchant_award
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_MERCHANT_AWARD_PORT:-50065}

  merchant_business:
    build:
      context: ../..
      dockerfile: merchant_business/Dockerfile
    image: monolith-ecommerce-grpc-vertx/merchant_business:latest
    container_name: merchant_business
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_MERCHANT_BUSINESS_PORT:-50066}

  merchant_detail:
    build:
      context: ../..
      dockerfile: merchant_detail/Dockerfile
    image: monolith-ecommerce-grpc-vertx/merchant_detail:latest
    container_name: merchant_detail
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_MERCHANT_DETAIL_PORT:-50067}

  merchant_policy:
    build:
      context: ../..
      dockerfile: merchant_policy/Dockerfile
    image: monolith-ecommerce-grpc-vertx/merchant_policy:latest
    container_name: merchant_policy
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_MERCHANT_POLICY_PORT:-50068}

  order:
    build:
      context: ../..
      dockerfile: order/Dockerfile
    image: monolith-ecommerce-grpc-vertx/order:latest
    container_name: order
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_ORDER_PORT:-50057}

  order_item:
    build:
      context: ../..
      dockerfile: order_item/Dockerfile
    image: monolith-ecommerce-grpc-vertx/order_item:latest
    container_name: order_item
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_ORDER_ITEM_PORT:-50056}

  product:
    build:
      context: ../..
      dockerfile: product/Dockerfile
    image: monolith-ecommerce-grpc-vertx/product:latest
    container_name: product
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_PRODUCT_PORT:-50058}

  review:
    build:
      context: ../..
      dockerfile: review/Dockerfile
    image: monolith-ecommerce-grpc-vertx/review:latest
    container_name: review
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_REVIEW_PORT:-50061}

  review_detail:
    build:
      context: ../..
      dockerfile: review_detail/Dockerfile
    image: monolith-ecommerce-grpc-vertx/review_detail:latest
    container_name: review_detail
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_REVIEW_DETAIL_PORT:-50069}

  shipping_address:
    build:
      context: ../..
      dockerfile: shipping_address/Dockerfile
    image: monolith-ecommerce-grpc-vertx/shipping_address:latest
    container_name: shipping_address
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_SHIPPING_PORT:-50063}

  slider:
    build:
      context: ../..
      dockerfile: slider/Dockerfile
    image: monolith-ecommerce-grpc-vertx/slider:latest
    container_name: slider
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=${GRPC_SLIDER_PORT:-50062}

  transaction:
    build:
      context: ../..
      dockerfile: transaction/Dockerfile
    image: monolith-ecommerce-grpc-vertx/transaction:latest
    container_name: transaction
    env_file: ./docker.env
    volumes:
      - ./docker.env:/app/docker.env
    networks:
      - app_network
    depends_on:
      pgbouncer:
        condition: service_healthy
      db-migration:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      otel-collector:
        condition: service_started
      redis-cluster-creator:
        condition: service_started
    environment:
      - OTEL_ENDPOINT=otel-collector:4317
      - APP_ENV=docker
      - KAFKA_BROKERS=kafka:9092
      - GRPC_PORT=50059

volumes:
  loki-data:
  grafana-storage:
  prometheus_data:
  postgres_data:
  kafka_data:
  redis_node_1_data:
  redis_node_2_data:
  redis_node_3_data:
  redis_node_4_data:
  redis_node_5_data:
  redis_node_6_data:

networks:
  app_network:
    driver: bridge
